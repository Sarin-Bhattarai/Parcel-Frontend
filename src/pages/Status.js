import React from "react";

const Status = () => {
  return (
    <div className="status-container">
      <input type="search" />
      <button>Search</button>
    </div>
  );
};

export default Status;
